import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#10172a",
  },
  sectionHeaderStyle: {
    paddingLeft: 16,
    color: "#fff",
    fontWeight: "bold",
    fontSize: 24,
    width: "100%",
    backgroundColor: "#10172a",
    paddingBottom: 8,
  },
});
